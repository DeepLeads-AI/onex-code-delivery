"""TEMPORARY — see TEMPORARY.md

A local stand-in for the platform's own neural-signal capture. Delete this whole
directory once the platform's endpoint records the requests it answers; nothing
in ``layer_profile/`` imports it except five CLI handlers marked ``# TEMPORARY``.
"""
