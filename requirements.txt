# Core — the metric itself. Pure numeric, no database, no model.
numpy==2.1.3
pandas==2.3.3
scipy==1.17.0

# [db] reading the platform's scalar rows
psycopg2-binary==2.9.11
python-dotenv==1.2.2

# [capture] the TEMPORARY local capture in platform_mimic/ — see TEMPORARY.md
torch==2.12.0
transformers==5.9.0
pyarrow==24.0.0

# [replay] posting at the live endpoint
requests==2.32.5

# [test]
pytest==9.0.3
